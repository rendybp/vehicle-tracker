# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/vehicle_tracker"

# Server Configuration
PORT=5000
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000

# JWT Secrets (Generate secure random strings for production)
# You can generate secrets using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_SECRET=your_access_token_secret_here_change_this_in_production
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here_change_this_in_production
